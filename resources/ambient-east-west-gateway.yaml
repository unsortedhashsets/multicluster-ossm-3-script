# Generated with https://github.com/istio/istio/blob/master/samples/multicluster/gen-eastwest-gateway.sh
# gen-eastwest-gateway.sh --mesh mesh1 --cluster west --network ${NET}

apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: istio-east-west
spec:
  controllerName: istio.io/mesh-controller

---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: istio-eastwestgateway
  namespace: istio-system
  labels:
    istio: eastwestgateway
    istio.io/rev: default
    topology.istio.io/network: ${NET}
spec:
  gatewayClassName: "istio-east-west"
  listeners:
    - name: mesh
      port: 15008
      protocol: HBONE
      tls:
        mode: Terminate
        options:
          gateway.istio.io/tls-terminate-mode: ISTIO_MUTUAL


